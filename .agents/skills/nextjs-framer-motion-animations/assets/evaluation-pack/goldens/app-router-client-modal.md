# Golden summary

- Change `components/settings-drawer.tsx`
- Keep the work in the existing client component
- Use `AnimatePresence` from `motion/react`
- Animate overlay and panel separately
- Mention stable keys, exit handling, and reduced-motion fallback
