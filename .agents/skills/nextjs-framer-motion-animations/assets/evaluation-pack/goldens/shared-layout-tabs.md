# Golden summary

- Change `components/settings-tabs.tsx`
- Use `LayoutGroup` plus `layoutId`
- Namespace repeated tab groups with `LayoutGroup id`
- Keep the change inside the existing client component
